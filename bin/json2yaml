#!/usr/bin/env python
#
# reads JSON from STDIN and outputs YAML to STDOUT
#
import sys
import yaml
import json
raw = sys.stdin.read()
obj = json.loads(raw)
yml = yaml.safe_dump(obj, default_flow_style=False)
sys.stdout.write(yml)
